# Context — record-learning-loop

## Base state

`main` is at `d12d9c0`. Twelve decision records, six archived tasks, no active work.

The problem logs of those six tasks are the evidence this task draws on. They contain roughly
twenty entries, of which two patterns recur across multiple tasks.

## References

- `decisions/0006-task-package-model.md` — decided the `## Problems` section and why friction
  is captured during work. It records the mechanism; what the mechanism feeds is unrecorded.
- `decisions/0012-build-the-methodology-before-the-tooling.md` — friction is the signal that
  tells which parts of the model earn their keep. That is the same loop, applied to the
  methodology rather than to a project using it.
- `OPEN-QUESTIONS.md` — holds the three agreed constraints for a learning layer: an observation
  is not automatically a rule, a lesson is a candidate, and a permanent behavior change
  requires a reviewed change.
- `README.md` — states the problem the loop answers, and stops there.

## Assumptions

- The loop is settled enough to record. It has run six times in the weak form — problems
  captured, patterns noticed, three rules changed as a result — even though no automation
  exists.
- Two instances of a pattern is coincidence and six is evidence. The recurring-patterns entries
  carry counts so a reader can judge that themselves rather than take the framing.

## Context conflicts

The source describes the loop as reaching `.agents/learning/`, with problems promoted to
lessons and lessons to improvements through dedicated directories. None of that exists. The
loop as practised runs through problem logs, review, and decision records, and the record
states what happens rather than what was drawn.

## Open questions

Whether the last step should ever be automated is deferred. This task records the loop and
adds the recurring patterns; it does not build the layer that would act on them.
