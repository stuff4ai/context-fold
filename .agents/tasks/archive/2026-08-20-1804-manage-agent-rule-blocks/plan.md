# Plan — manage-agent-rule-blocks

1. Record the managed-block ownership decision and narrow only the Status of accepted records
   whose whole-file claims it replaces.
2. Put every portable rule document inside one generic managed block and update the canonical
   adoption procedure, skill, root workflow, README, and live questions.
3. Reinstall the complete skill, use its new repeat-adoption flow to update this repository's four
   active blocks, and preserve installation-owned suffixes.
4. Replace whole-file dogfooding checks with managed-block structure and three-copy parity checks,
   keeping whole-package skill parity unchanged.
5. Exercise suffix and malformed-marker behavior with disposable fixtures, run all repository
   checks, fold the outcome, archive the task, and submit the finished state for human approval.
