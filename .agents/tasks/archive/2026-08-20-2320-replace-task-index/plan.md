# Plan

1. Capture a migration oracle for every current task: status, normalized objective, title,
   remaining body, and hashes of all other accepted-archive files.
2. Define strict task frontmatter in the portable rules and convention tests, then mechanically
   migrate the current corpus, including this task.
3. Update the distribution and adoption procedure to create new-format task zero and no index;
   update project documentation and the replacement decision without touching PR #39's sections.
4. Reinstall the complete skill, invoke its installed copy, and verify managed-block and skill
   parity.
5. After PR #39 resolves, rebase onto refreshed `main`, record the integration base, preserve its
   suffix and records, migrate its task if merged, apply the deferred edits, and delete the index.
6. Re-run the migration oracle and full checks, fold durable outcomes out, archive this package,
   perform an exact-head review, and open the Human-approved pull request.
